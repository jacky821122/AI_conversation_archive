# Phase B RAG 生成端設定（OpenAI 相容介面）
# 用法：cp .env.example .env，再填入自己的金鑰。.env 已 gitignore。

# 必填：生成端 API 金鑰（只有檢索到的片段會連同問題送往此端點）
AGNES_API_KEY=

# 選填：覆蓋預設。預設打 agnes AI 的 agnes-2.0-flash。
# 日後要換回 Claude 或別家，改這三個即可（需 OpenAI 相容端點）。
AGNES_BASE_URL=https://apihub.agnes-ai.com/v1
AGNES_MODEL=agnes-2.0-flash
